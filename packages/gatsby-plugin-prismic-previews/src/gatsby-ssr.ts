/**
 * APIs exported here must be re-exported manually in the root `gatsby-ssr.js`
 * file. Gatsby will not detect the exports if the module is re-exported as a
 * whole.
 */

export { wrapRootElement } from './wrap-root-element'
export { onRenderBody } from './on-render-body'
