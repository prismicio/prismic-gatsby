// This global variable is used by Gatsby's `withAssetPrefix` helper to build
// a URL using the site's configured asset URL or directory.
// eslint-disable-next-line no-var
declare var __PATH_PREFIX__: string;
