{
  "name": "ts-prismic",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "prebuild": "rimraf dist",
    "build": "microbundle build src/index.ts --tsconfig tsconfig.build.json",
    "dev": "microbundle watch src/index.ts --tsconfig tsconfig.build.json",
    "format": "prettier --write '{src,docs}/**/*.{ts,tsx,md}'",
    "test": "jest",
    "prepare": "yarn build",
    "release": "standard-version"
  },
  "dependencies": {
    "type-fest": "^0.20.2"
  }
}
